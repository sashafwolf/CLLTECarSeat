# Collette dependency snapshot -- generated 2026-08-15 from the real
# interpreter she runs under (Python 3.11, C:\Users\...\Programs\Python\Python311).
# Install with: pip install -r requirements.txt
#
# Stdlib modules used (no install needed): os, sys, json, socket, webbrowser,
# time, datetime, subprocess, threading, uuid, re, hashlib, shlex, sqlite3,
# atexit, logging, traceback, urllib.parse, functools, typing.
#
# nltk needs one extra one-time step after install: nltk.download('vader_lexicon')
# -- bastet_descendant_soul.py already does this automatically on first boot
# (needs internet access that one time).

requests==2.34.2
psutil==7.2.2
chromadb==1.5.9
trafilatura==2.0.0
beautifulsoup4==4.14.3
APScheduler==3.11.2
nltk==3.9.4
ddgs==9.14.4
youtube-transcript-api==1.2.4
Flask==3.1.3
python-dotenv==1.2.2
google-genai==2.6.0
anthropic==0.122.0
openai==3.0.0
discord.py==2.7.1
aiohttp==3.13.5
